   A := 1.0;
   B := 2.0;
   C := 1.0;
   X  := 0.0;
   X1 := 0.0;
   X2 := 0.0;   
   SD := sqrt (B * B - 4.0 * A * C);
   X1 := (- B + SD) / (2.0 * A);
   X2 := (- B - SD) / (2.0 * A);
   Put_Line (X1);
   Put_Line (X2); 
 
